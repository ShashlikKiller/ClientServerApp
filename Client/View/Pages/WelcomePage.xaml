<Page x:Class="Client.View.Pages.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WelcomePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <TextBlock Text="Добро пожаловать!" FontSize="80" HorizontalAlignment="Center" Foreground="AliceBlue" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel Grid.Row="1">
            <TextBlock Text="Выберите студента из списка слева для просмотра его информации." FontSize="20" HorizontalAlignment="Center" Foreground="AliceBlue" VerticalAlignment="Top"/>
        </DockPanel>
        <DockPanel Grid.Row="1">
            <TextBlock Text="Также вы можете изменить его информацию или удалить его." FontSize="15" HorizontalAlignment="Center" Foreground="AliceBlue" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Page>
